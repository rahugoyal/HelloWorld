plugins {
    id "com.android.application"
}

android {
    namespace "com.example.hellocicd"
    compileSdk 36

    defaultConfig {
        applicationId "com.example.hellocicd"
        minSdk 23
        targetSdk 36
        versionCode 1
        versionName "1.0.0"
    }
}

tasks.register("printVersionName") {
    doLast {
        println android.defaultConfig.versionName
    }
}
